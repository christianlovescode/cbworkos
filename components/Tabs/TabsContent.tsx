import * as TabsPrimitive from "@radix-ui/react-tabs";
import { styled } from "@/config/stitches.config";

const StyledContent = styled(TabsPrimitive.Content, { height: "100%" });

export const TabsContent = StyledContent;
